## Summary

`/home/winger/uya/uya/bin/uya test` does not emit a C `main` symbol for a minimal test entry file on Uya `v0.9.9`, so the host linker fails with `undefined reference to main`. The same toolchain also prevents HyperGit's existing `make test` gate from running.

## Affected Tasks

- 更激进的服务端索引、云端查询和分布式执行能力。

## Toolchain Command

`/home/winger/uya/uya/bin/uya test .agent/toolchain-bugs/repros/20260602-100355-uya-test-entry.uya`

## Actual Error

The Uya compiler type-checks and generates C, then host linking fails:

```text
/usr/bin/ld: /usr/lib/gcc/x86_64-linux-gnu/12/../../../x86_64-linux-gnu/Scrt1.o: in function `_start':
(.text+0x17): undefined reference to `main'
collect2: error: ld returned 1 exit status
错误：宿主工具链链接失败，返回码：256
错误：宿主工具链链接失败
```

`make test` is also blocked at the first existing project Uya test with `Segmentation fault` during `uya test src/hypergit/test_object_model.uya`.

## Expected Behavior

`uya test` should generate or link a valid executable entry point for a test file that imports `std.runtime.entry` and exports `main`, then run the test executable successfully.

## Repro File

`.agent/toolchain-bugs/repros/20260602-100355-uya-test-entry.uya`

## Repro Code

```uya
use std.runtime.entry;

export fn main() i32 {
    return 0;
}
```

## Notes

Observed with `/home/winger/uya/uya/bin/uya --version` reporting `v0.9.9`. `uya check src/hgx/main.uya` still passes, but checker-only validation is not enough to complete the affected TODO because it does not execute tests.
